<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/advance_setting_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <!-- Transparent streaming background to avoid extra overdraw -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="KeyboardKeyStyle">
        <item name="android:background">@drawable/square_border</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#B3FFFFFF</item> <!-- 字体颜色 -->
        <item name="android:gravity">center</item>
    </style>

    <style name="PreferenceThemeWithShadow" parent="AppTheme">
        <item name="android:textViewStyle">@style/PreferenceTextWithShadow</item>
        <item name="android:titleTextAppearance">@style/PreferenceTitleWithShadow</item>
        <item name="android:subtitleTextAppearance">@style/PreferenceSummaryWithShadow</item>
        <item name="android:colorAccent">@color/theme_pink_secondary</item>
        <item name="android:textColorPrimary">@color/theme_pink_secondary</item>
        <item name="android:listDivider">@color/theme_pink_secondary</item>
    </style>

    <style name="PreferenceTextWithShadow">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:shadowColor">#88000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">3</item>
        <item name="android:letterSpacing">0.03</item>
    </style>

    <style name="PreferenceTitleWithShadow" parent="PreferenceTextWithShadow">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="PreferenceSummaryWithShadow" parent="PreferenceTextWithShadow">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/theme_pink_light</item> <!-- 柔和的浅蓝色 -->
    </style>

    <style name="PreferenceCategoryWithShadow" parent="PreferenceTextWithShadow">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/theme_pink_secondary</item> 
        <item name="android:shadowColor">#AA000000</item> <!-- 更强的阴影 -->
        <item name="android:shadowRadius">5</item> <!-- 更大的阴影半径 -->
    </style>

    <!-- GameMenu 对话框样式 -->
    <style name="GameMenuDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/game_menu_dialog_bg</item>
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">576dp</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/GameMenuDialogAnimation</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <!-- GameMenu 按钮样式 -->
    <style name="GameMenuButtonStyle" parent="android:Widget.Button">
        <item name="android:background">@drawable/game_menu_button_bg</item>
        <item name="android:textColor">@color/game_menu_text_primary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">6dp</item>
        <item name="android:paddingRight">6dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:gravity">center</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:elevation">0dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:drawablePadding">4dp</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <!-- GameMenu 列表项样式 -->
    <style name="GameMenuListItemStyle" parent="android:Widget.ListView">
        <item name="android:background">@drawable/game_menu_list_item_bg</item>
        <item name="android:textColor">@color/game_menu_text_primary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:padding">16dp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- GameMenu 对话框动画 -->
    <style name="GameMenuDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/game_menu_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/game_menu_dialog_exit</item>
    </style>

    <!-- 通用弹窗样式 -->
    <style name="AppDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/game_menu_dialog_bg</item>
        <item name="android:windowMinWidthMajor">60%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/AppDialogAnimation</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:textColorPrimary">@color/theme_pink_primary</item>
        <item name="android:textColorSecondary">@color/game_menu_text_secondary</item>
        <item name="android:colorAccent">@color/theme_pink_primary</item>
        <item name="android:listDivider">@color/theme_pink_primary</item>
    </style>

    <!-- 通用弹窗动画 -->
    <style name="AppDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/game_menu_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/game_menu_dialog_exit</item>
    </style>

    <!-- 通用按钮样式 -->
    <style name="AppDialogButtonStyle" parent="android:Widget.Button">
        <item name="android:background">@drawable/game_menu_button_bg</item>
        <item name="android:textColor">@color/theme_pink_primary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:elevation">0dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- 进度对话框样式 -->
    <style name="AppProgressDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/game_menu_dialog_bg</item>
        <item name="android:windowMinWidthMajor">50%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/AppDialogAnimation</item>
        <item name="android:textColorPrimary">@color/theme_pink_primary</item>
        <item name="android:colorAccent">@color/theme_pink_primary</item>
        <item name="android:progressTint">@color/theme_pink_primary</item>
        <item name="android:indeterminateTint">@color/theme_pink_primary</item>
        <item name="android:indeterminateDrawable">@drawable/app_progress_indeterminate</item>
    </style>
</resources>
