# Moonlight V+ 隐私说明

本说明适用于 Moonlight V+ Android 客户端。不同发行包的 Firebase 配置可能不同；自行编译且未提供 `app/google-services.json` 的版本通常无法连接 Firebase。主机串流、统计分析和崩溃诊断是不同的数据路径。

## 串流与设备功能

- 应用与用户选择的主机建立连接，传输画面、音频和输入操作。主机名称、配对信息、设置和缓存保存在设备上；用户主动导出配置或分享文件时，由用户选择接收方。
- 启用麦克风重定向并授予录音权限后，麦克风音频会发送到所连接的主机。此功能默认关闭，可在设置和串流中控制；应用不会把这段音频作为统计事件上传到 Firebase。
- 相机权限用于扫码配对；蓝牙、USB、传感器、通知及文件访问权限用于相应的可选功能。具体可用权限由 Android 版本和设备决定。

## 统计分析

含有效 Firebase 配置的正式版使用 Firebase Analytics。应用自身记录启动和会话、串流开始与结束、时长，以及主机名称、应用名称、解码器、分辨率和平均延迟等事件参数；还会记录首次打开日期及是否完成过首次串流等用户属性。主机名称和应用名称可能由用户自行设置，因此这些数据不能保证完全匿名。Firebase SDK 也可能记录其自动事件及设备相关数据，详见 [Firebase 隐私与安全说明](https://firebase.google.com/support/privacy)。

“参与统计”设置默认开启。关闭后，应用自身的 `AnalyticsManager` 不再发送上述自定义事件和用户属性；目前该开关没有调用 Firebase SDK 的全局停用接口，因此**不能视为已关闭 Firebase 自动收集或 Crashlytics**。Debug 构建不执行应用自身的这些统计调用。我们会在调整实际开关行为后同步更新此说明。

## 崩溃诊断

含有效 Firebase 配置且服务可用的版本集成 Firebase Crashlytics，可能自动向 Google 发送崩溃诊断。它与“参与统计”开关分开运作。

应用还会在本机保存最近一次未捕获异常的报告，包含时间、应用版本、设备与 Android 信息、线程名称和异常堆栈。下次启动时，用户可查看并选择分享、复制或忽略；本地报告不会因为生成而自动通过这条分享流程发送给开发者。分享操作会交给用户选择的其他应用处理。忽略或分享后，应用会尝试删除本地报告。

## 保存、控制与联系

设备上的配对信息、配置和缓存会按对应功能保存；卸载应用通常会移除应用私有存储，但不代表此前已发送到 Firebase 的数据会同步删除。Firebase 数据的保留时间取决于项目设置及服务规则，本仓库代码不能证明固定的保留期限。

如需询问数据处理或请求删除已上报的数据，请通过 [项目 Issues](https://github.com/qiin2333/moonlight-vplus/issues) 联系维护者，并避免在公开 Issue 中张贴配对证书、主机地址、完整崩溃日志等敏感内容。能够定位和删除的数据范围取决于实际可用的标识及 Firebase 服务能力。

本说明最后更新于 2026 年 9 月。应用行为如有变化，应同步更新本页。
