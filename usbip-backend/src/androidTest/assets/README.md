These certificates and unencrypted PKCS#8 keys are generated test fixtures only.
They are public, deliberately disposable, and must never be paired with a user's
Sunshine installation or copied into production assets. They are packaged only
in the instrumented test APK. The test server requires the test client certificate;
the client pins the exact test server certificate.
